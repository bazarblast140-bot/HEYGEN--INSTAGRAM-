# HeyGen API key -> https://app.heygen.com/settings?nav=API
HEYGEN_API_KEY=your_heygen_api_key_here

# Server port (optional, default 3000)
PORT=3000

# Default avatar / voice used when the UI or CLI does not send one (optional)
DEFAULT_AVATAR_ID=
DEFAULT_VOICE_ID=
